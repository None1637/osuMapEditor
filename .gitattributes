# 默认: 自动识别文本文件并统一为 LF
* text=auto eol=lf

# Windows 批处理必须 CRLF, 否则部分语法 (goto/标签) 会失效
*.bat text eol=crlf

# 二进制资源不参与换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp3 binary
*.wav binary
*.ogg binary
*.exe binary
*.osz binary
